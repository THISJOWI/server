spring:
  application:
    name: password
  cloud:
    config:
      uri: http://config:8888
      fail-fast: false
      retry:
        initial-interval: 1000
        max-attempts: 6
        max-interval: 2000
        multiplier: 1.1

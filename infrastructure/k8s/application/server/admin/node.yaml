apiVersion: v1
kind: Service
metadata:
  name: admin-nodeport
  labels:
    app: admin-nodeport
spec:
  selector:
    app: admin
  type: NodePort
  ports:
    - name: http
      protocol: TCP
      port: 8090     
      targetPort: 8090 
      nodePort: 30090 
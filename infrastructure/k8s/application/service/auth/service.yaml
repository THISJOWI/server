apiVersion: v1
kind: Service
metadata:
  name: auth
  labels:
    app: auth
spec:
  selector:
    app: auth
  ports:
    - name: http
      protocol: TCP
      port: 8082         # port within the cluster
      targetPort: 8082    # container port
  type: ClusterIP